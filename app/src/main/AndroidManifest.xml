<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cst438_project1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainMenuAtivities.ViewAssignment.deleteAssignment"></activity>
        <activity android:name=".MainMenuAtivities.ViewAssignment.editAssignment" />
        <activity android:name=".MainMenuAtivities.ViewAssignment.addAssignment" />
        <activity android:name=".CreateAccount" />
        <activity android:name=".Login" />
        <activity android:name=".MainMenuAtivities.ViewGradesActivity" />
        <activity android:name=".MainMenuAtivities.AccountActivity" />
        <activity android:name=".MainMenuAtivities.ViewAssignment.ViewAssignmentActivity" />
        <activity android:name=".MainMenuAtivities.CourseViewerActivity" />
        <activity android:name=".MainMenuAtivities.EditAccountActivity"/>
        <activity android:name=".mainMenu" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>